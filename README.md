# Open Source - (public data) Emissions Forecasting

1. Download the **India dataset** from the Climate TRACE data portal:
   https://climatetrace.org/data

2. Unzip the downloaded files and update the input file path in `IND_OpenSource_final.ipynb` if required.

3. Run `IND_OpenSource_final.ipynb`.

The notebook contains the complete data preparation, feature engineering, model training, forecasting, and evaluation pipeline used in the thesis.
# Open Source - (public data) Emissions Forecasting - Deep Learning Models
1. Run IND_OpenSource_final.ipynb with the file created in the above step
